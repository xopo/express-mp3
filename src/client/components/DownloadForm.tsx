
type DFProps = {
    onComplete: React.Dispatch<React.SetStateAction<boolean>>,
    setVisible: React.Dispatch<React.SetStateAction<boolean>>;
}
export default function DownloadForm({onComplete, setVisible}: DFProps) {
    return (
        <h3>Download form not implemented</h3>
    );
}